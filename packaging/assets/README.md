# Packaged app assets

| File | Source | Use |
|------|--------|-----|
| `josvn-icon.ico` | `frontend/public/brand/josvn-icon.png` | PyInstaller exe icon, window title bar, taskbar |

Regenerate after updating the PNG:

```bash
make build-icon
```
