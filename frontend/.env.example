# Desktop (make dev / make start): login HTTP is handled by Python — set AUTH_API_URL in repo .env or environment.
# Browser-only (make local): optional override for direct fetch from the frontend.
# VITE_AUTH_API_URL=https://script.google.com/macros/s/.../exec
